(function() {
  
	/**
	 * 	@name	app.title
	 * 	@desc	A module for updating the HTML page title to allow sensible 
	 * 			bookmarks.
	 */
	angular
		.module("app.title", [
			"ui.router"
		]);
  
}());
