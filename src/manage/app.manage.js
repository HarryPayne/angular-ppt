(function() {
  
	/**
	 * 	@name app.manage
	 *  @desc A module to support the functions under the Manage tab.
	 *  		In the legacy app this tab was used to manage user roles,
	 *  		and other tasks requiring the "Manage" role. 
	 */
	angular
		.module("app.manage", [
			"ui.router"
		]);
  
}());
